# RebuiltTux2 Kernel

Current kernel stage:

- GRUB multiboot entry point
- Memory manager initialization
- Future VFS initialization
- Future device drivers

Build target:

```
kernel.bin
```

Loaded by GRUB from:

```
/boot/kernel.bin
```
